One-line: icon-only square button for toolbars and dismiss affordances; always pass `label`.

```jsx
<IconButton name="download" label="Download PDF" />
<IconButton name="grid-2x2" label="Grid view" variant="outline" active />
```

Same 26/32/40px heights as Button so they align in a row. `active` tints the glyph accent-spring.
