One-line: the wordmark — `JAMES NOTLEY` in bold mono, role qualifier, and a blinking spring-green cursor.

```jsx
<Logotype size={18} />           {/* header */}
<Logotype size={52} gradient variant="name" />  {/* hero */}
<Logotype variant="initials" size={14} />       {/* favicon, print footer */}
```

No graphic mark exists. Never draw one; never stretch or recolour the cursor block.
