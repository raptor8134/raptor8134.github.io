One-line: filterable keyword chip; content is lowercase and technical.

```jsx
<Tag selected>ti-6al-4v</Tag>
<Tag onRemove={() => {}}>sem</Tag>
```

Selected state uses the soft gradient wash plus an accent rule. Keep labels lowercase, hyphenated.
