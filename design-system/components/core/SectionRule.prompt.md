One-line: uppercase label + 1px rule running to the container edge; opens every section on site and print.

```jsx
<SectionRule index="02" label="Selected work" />
<SectionRule label="Contact" gradient />
```

At most two gradient rules per page — usually the hero and the closing section.
