One-line: path trail rendered as a shell path — green `~`, faint slashes, current segment in strong text.

```jsx
<Breadcrumb items={["work","lattice-inserts"]} />
```

Segments are lowercase and hyphenated, like real paths. `size="lg"` turns the path into the page
heading on article pages — the file-tree bar IS the title there.
