One-line: the portfolio's global header — translucent, blurred, one hairline bottom rule.

```jsx
<SiteHeader links={["work","about","writing","contact"]} active="work" onNavigate={go} />
```

Height is fixed at 56px. Never stack a second nav row; use Tabs inside the page instead.
