One-line: category switcher; 2px spring underline plus a soft gradient wash on the active tab.

```jsx
<Tabs items={[{value:"all",label:"All",count:12},{value:"metals",label:"Metals",count:5}]}
      value={tab} onChange={setTab} />
```
